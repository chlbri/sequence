{
  "name": "Node.js Library",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:24",
  // 3 GB RAM for the container
  "runArgs": ["--memory=3g", "--memory-swap=3g", "--cpus=2"],
  "hostRequirements": {
    "memory": "3gb"
  },
  // pnpm via corepack (ships with Node 24) + install deps
  "postCreateCommand": "corepack use pnpm@latest && pnpm install --frozen-lockfile",
  // Forward vitest --ui port, also Ollama
  "forwardPorts": [51204, 11434],
  "portsAttributes": {
    "51204": {
      "label": "Vitest UI",
      "onAutoForward": "notify"
    },
    "11434": {
      "label": "Ollama",
      "onAutoForward": "notify"
    }
  },
  "remoteEnv": {
    "NODE_ENV": "development",
    // Give Node.js ~2 GB for heap
    "NODE_OPTIONS": "--max-old-space-size=2048",
    // pnpm store inside the container (no cross-device issues)
    "PNPM_HOME": "/home/node/.local/share/pnpm"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "editor.formatOnSave": true,
        "editor.defaultFormatter": "oxc.oxc-vscode",
        "coverage-gutters.coverageFileNames": [".coverage/lcov.info"],
        "vitest.enable": true,
        "vitest.commandLine": "pnpm run test:only"
      },
      "extensions": [
        "ms-vscode.vscode-typescript-next",
        "ms-vscode.js-debug",
        "oxc.oxc-vscode",
        "vitest.explorer",
        "ryanluker.vscode-coverage-gutters",
        "adam-bender.commit-message-editor",
        "github.vscode-github-actions",
        "github.copilot",
        "github.copilot-chat",
        "anthropic.claude-code",
        "Gruntfuggly.todo-tree",
        "TypeScriptTeam.native-preview"
      ]
    }
  }
}
